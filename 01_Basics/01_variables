let a='l';

if(1)
{
//let a='v';
a='k';
console.log(a);
}
// A variable is a container for storing data values like numbers, strings, or objects.
// ✅ let – Block Scoped Variable
let name = "Alice";
name = "Bob"; // You can reassign
console.log(name); // Output: Bob
//It’s block scoped (only accessible inside {} where it’s defined).
//✅ const – Constant Variable
                 const PI = 3.14;
               // PI = 3.14159; ❌ Error: Assignment to constant variable.
/*•  You cannot reassign.
•  It’s also block scoped.
•  BUT if it's an object or array, you can change the content*/
const person = { name: "Alice" };
person.name = "Bob"; // ✅ Works
console.log(person.name); // Bob





//console.log(a);





